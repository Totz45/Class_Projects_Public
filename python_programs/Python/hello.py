name = input("Hi mom\n") # similar to cin >> name;
print(f"How are you? {name}") # similar to cout << name;
"""
+ adds to function, 
"""
#.strip() removes whitespace from str
#.capitalize() capitalize first letter, .title() capitalize first letter of every word, can use multiple
#can use firt, last = name.split().
#.rstrip() removes newline from str